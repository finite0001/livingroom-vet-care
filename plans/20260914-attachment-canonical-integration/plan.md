# Integrate reviewed API originals on the deployed-compatible stack

Status: active; implementation and hosted parity unproven.

## Authoritative baseline

Read-only staging checks on2026-09-14 found84 migrations, deployed6300 `record_release_prescription_history`, schema8 present, schema9 and alternative attachment captures absent, and zero Auth users/clients/pets/Storage objects/outbox rows. Seven export functions are version5. This supersedes this thread's earlier75-migration staging assumption.

Start from canonical capture revision74d0c0c, which preserves deployed6300 and additive6500 prescription hardening, metadata6900 and capture7000. Preserve its stable metadata hashes, separate file IDs, owner/history recovery, URL redaction, currentness and bounded streaming. Compare with alternative f8c63ca in the preserved `livingroom-readiness-security` worktree. Do not merge its replacement6300 or recreate schema8 via6400. Same-version semantic collisions also exist at6500,6900 and7000; exact paths/hashes are in `docs/evidence/attachment-canonical-preflight-20260914.json`.

The alternative branch contains useful reviewed-original/chart/schema9 behavior and tests, but its local102-migration evidence is not proof of compatibility with staging. Preserve that work as a reference and port behavior explicitly. Do not discard canonical prescription reconciliation validation, partial-history UI disclosures or post-restore relation/RLS/default-privilege checks.

## Implementation sequence and acceptance

1. Map canonical capture request/intent/receipt and metadata observation contracts to alternative approval, correction, cancellation and release provenance. Document every identity/hash/status difference before SQL changes. Use `ezyvet_attachment_original_captures` and its saved request/observation context; do not create a competing capture store or duplicate originals. Identify which private original access can safely be expanded from owner to active staff after approval.
2. Add immutable approval/correction/cancellation and exact retry/recovery against canonical ready captures. Use unused additive migration versions after reviewing the current remote ledger. Retain predecessor/source lock order, actor scoping, original inspection and explicit attestation. Port meaningful contention tests, including revoked roles during waits and cancellation versus delayed approval.
3. Integrate reviewed originals into the patient chart with bounded discovery, latest/superseded and source-currentness distinctions, verified byte access and existing draft/navigation guards. Retain current buttons, routes and layout. Unreviewed originals stay owner scoped.
4. Add schema9 through the canonical schema8 implementation. Preserve canonical prescription validators/renderers and partial-history disclosures. Select exact approved records; compose mixed native/API originals; enforce20API/24total limits; register invalidation; bind both SQL and worker artifact verification to canonical captures. Port replay/read/public-link post-wait authorization fixes as additive checked changes, not reused migration numbers.
5. Integrate both email and document-link preparation, recovery and worker paths. Reuse canonical capture source identity and private bucket. Verify swapped/missing/duplicate bytes, lost replies, source invalidation, role/expiry lock waits, uncertain delivery and exact saved recovery without automatic resend. Use simulated transports only until explicitly authorized provider commissioning.
6. Validate parent contracts against canonical provider documentation and accepted samples. Preserve Animal support. Resolve Consult/Contact contracts explicitly; alternative synthetic Consult fixtures do not establish vendor entitlement or accepted real-source semantics. Implement required supported parent paths with independent recovery/currentness evidence.
7. Run integrated checks on this reconciled stack: unit, browser, SQL, observed contention, actual local Auth/HTTP/Storage, mixed/max-count packages and populated upgrade/physical restore. Restore checks must include public routines, permissions, triggers, relations, RLS policies and default privileges after restoration, plus original bytes, approval/cancellation, saved delivery artifacts, source changes and disabled-policy behavior.
8. Revalidate staging immediately before coordinated migration/function/frontend updates. Preserve the deployed6300 receipt. Rehearse the actual84-version baseline and exact additive bridge locally; compare hosted bodies/permissions with expected baseline first. Keep sends, import scopes, reminders and clinical policy disabled. Hosted staff workflow, clinician/provider acceptance and public cutover remain separate gates in `docs/commercial-readiness.md`.

## Exit evidence

A migration-safe integrated branch with retained canonical safeguards; hash-bound current-stack validation and populated84-to-target upgrade/restore; exact coordinated staging parity; actual authorized staff/clinical/provider acceptance. The broad commercial-readiness goal still includes Fastmail/Auth onboarding, Stripe sandbox/live eligibility, lab/anesthesia contracts, stock/opening balances, monitoring and domain/brand acceptance. This plan does not shrink that goal to attachment tests.

First increment: [capture contract mapping](capture-contract-map.md) is complete. Additive20260914010000 implements canonical ready-capture approval/correction and exact recovery without changing Storage policies. Evidence: `docs/evidence/canonical-api-approval-sql-20260914.json` (432 SQL assertions including14 new,72 existing capture contention checks, owned cleanup). Approval-specific races, cancellation and subsequent integration steps remain required.

Cancellation increment: additive20260914020000 implements serialized cancellation and exact terminal recovery on canonical captures, preserving committed approvals and denying access to the uncancelled helper. Evidence: `docs/evidence/canonical-api-cancellation-local-20260914.json` (441 SQL assertions,9 new;88 concurrency checks,16 new; exact holder/waiter relations; owned cleanup). Both decision orderings and role loss during approval/cancellation operation waits pass. Remaining approval source/request/chain/object races, discovery/UI/chart and schema9 integration are still required.

Approval-boundary acceptance (2026-09-14): `docs/evidence/canonical-api-approval-boundaries-20260914.json` records441 SQL assertions and117 concurrency checks (29 new). Role loss at request/source/chain/object waits, both source-revision/approval orderings, exact historical replay and competing corrections pass with exact holder/waiter verification and owned cleanup. Together with decision-lock/cancellation cases, this completes the planned approval/cancellation SQL race checkpoint. UI/chart, canonical schema9 and its release races, actual HTTP/Storage integration and populated restore remain required.

Review-history increment (2026-09-14): additive20260914030000 provides bounded owner-scoped discovery on canonical ready captures, omitting source context. The existing capture panel now displays latest/earlier reviews, pagination and recoverable errors. The response parser binds patient/mapping/source and selected-capture hashes, rejects malformed cursors, and compares timezone-normalized timestamps at PostgreSQL microsecond precision. Evidence: `docs/evidence/canonical-api-review-history-20260914.json`;450 SQL assertions (9 new),117 existing concurrency checks,519 unit tests (3 new),14 capture browser tests (1 new),lint/typecheck/build passed with existing warnings. This is read-only history UI; explicit approval/inspection/cancellation controls, chart/schema9, physical Storage and hosted commissioning remain outstanding.

Decision-form increment (2026-09-14): staff approval/correction controls now require a verified original download and inspection attestation, preserve an exact operation before submission, recover approved/canceled outcomes, explicitly cancel unresolved decisions and retain draft/navigation locks. The client validates canonical file ID, ordinal, metadata, both source hashes and exact operation identity. Reload acceptance exposed a run/capture recovery deadlock; allowing read-only recovery of the same retained metadata run resolves it without enabling new requests. `docs/evidence/canonical-api-decision-ui-20260914.json` records522 unit tests (3 new),28 attachment browser tests (3 new),lint/typecheck/build passed with existing warnings. Browser acceptance covers lost response plus reload, cancellation, and signout during fresh history check. No SQL/hosted changes; actual HTTP/Storage approval integration, additional correction/currentness browser cases, patient chart/schema9 and populated restore remain outstanding.

Patient-chart history increment (2026-09-14): additive20260914040000 exposes only approved records to active staff. It omits source context/private paths, retains superseded reviews and uses canonical capture currentness independently of latest-review status. The patient page adds this bounded history beside existing documents, with patient/session-scoped validation and preserved dirty-state controls. Evidence: `docs/evidence/canonical-api-chart-history-20260914.json`;463 SQL assertions (13 new),117 existing contention checks,525 units (3 new),18 browser tests including expanded mobile patient acceptance. Lint/typecheck/build passed with existing warnings. Download access is deliberately not represented as complete: canonical capture transport currently authorizes owner admins, so reviewed-file retrieval needs a separately checked active-staff server path without adding broad Storage SELECT. Patient-chart verified bytes, actual HTTP/Storage, schema9 and populated restore remain outstanding.

Reviewed chart download increment (2026-09-14): additive20260914050000 provides service-only context bound to active reader, exact review/patient/capture, canonical intent and Storage object metadata. The new `retrieve-reviewed-ezyvet-original` handler verifies bytes and rechecks token, active-staff context and file identity after reading. The chart downloads binary data with client checksum/signature verification and suppresses late results after signout/navigation. Storage SELECT policies remain unchanged. `docs/evidence/canonical-reviewed-original-download-20260914.json` records471 SQL assertions (8 new),117 existing concurrency checks,528 units (3 new handler tests),Edge typecheck and expanded mobile patient/browser acceptance including JPEG bytes, tampering and delayed signout. The browser test required explicit CORS exposed headers to match the real handler and desktop resize to use the existing Sign Out control after mobile checks. Actual local Auth/HTTP/Storage roundtrip, hosted endpoint deployment and remaining chart/correction acceptance are pending; schema9 and populated restore remain outstanding.

Actual local Auth/Storage increment (2026-09-14): disposable acceptance now requires the exact92-migration canonical stack and fingerprints capture, decision, history and reviewed-download dependencies. Combined acceptance passed120 checks (50 metadata,70 originals; original baseline was52). Real PostgREST approval responses pass client provenance validation; corrections/cancellation, non-owner STAFF history/retrieval, direct Storage denial, deactivation during actual Storage read, historical source/household changes and physical same-size corruption pass. Evidence: `docs/evidence/canonical-api-auth-storage-20260914.json`; runner verified container/volume/private-file cleanup and unchanged test sources. Production handlers ran via local Node HTTP bindings, not deployed Edge routing; no actual ezyVet/provider/clinical acceptance. Current-stack schema9 and mixed release integration, populated84-to-target upgrade/physical restore and coordinated hosted commissioning remain required.

Release-selection prerequisite (2026-09-14): additive20260914060000 validates1–20 exact distinct approval ID/hash references, current latest same-patient provenance, canonical capture/intent metadata and exact Storage object identity. The helper is private to composition and rechecks active staff after locks. Stable lock ordering covers mapping/patient, parent/attachment heads, approval chains and Storage objects. Its output retains canonical `bucket_id`, `intent_id`, separate file ID, ordinal and both metadata hashes; full raw metadata is omitted from release provenance. `docs/evidence/canonical-release-attachment-validation-20260914.json` records482 SQL assertions (11 new),140 contention checks (23 new),seven new observed wait cases and cleanup. The disposable runner now requires93 migrations; its HTTP suite will be rerun for the integrated schema9 target. Schema9 preview/selection, canonical prescription validation, mixed24-file packages, invalidation, byte verification, delivery and populated upgrade/restore remain required. No release policy or API activation occurred.

Private schema9 preview increment (2026-09-14): additive20260914070000 maps canonical v2 approvals/capture-v1 originals to release file references and composes them with the existing schema8 preview. It retains schema8 prescription validation, validates explicit sources/recipient, limits20API selections and24 total distinct originals, bounds snapshot size and hashes the complete snapshot. API-only and native-weight/API composition pass; the native weight projection is identical to schema8, and an invalid mixed prescription remains rejected. `docs/evidence/canonical-api-release-preview-20260914.json` records500 SQL assertions (18 new),140 existing concurrency checks and cleanup. No public preview/confirmation dispatch or schema9 policy activation was added. Confirmation/recovery, invalidation, original-byte verification, worker/UI composition, positive mixed prescription and maximum-count physical files, delivery and populated upgrade/restore remain required. Disposable runner target is now94 migrations.

Schema9 byte-contract increment (2026-09-14): additive20260914080000 preserves the current native verifier in a private legacy helper and adds strict schema9 approval-selection/file-descriptor cardinality, patient, length and SHA-256 checks. API originals cannot be omitted, duplicated or downgraded to ordinary native files. Native captured-source verification delegates through a local schema8 view without changing the saved snapshot. `docs/evidence/canonical-api-release-bytes-20260914.json` records all73 SQL files/2,799 assertions,162 prescription harness checks and204 attachment harness checks, plus owned cleanup; these harness totals include setup/SQL-file checks and are not counts of distinct races. New tests include valid/tampered native captured-source files in mixed packages. No public schema9 confirmation or policy activation occurred. Confirmation/recovery, invalidation, UI/worker package composition, max-count physical files, delivery and populated upgrade/restore remain outstanding. Disposable runner target is95 migrations.

Public schema9 confirmation/recovery increment (2026-09-14): additive20260914090000 connects authenticated preview, exact confirmation/replay, saved-read dispatch and registered API source invalidation while retaining canonical schema8 and leaving clinical policy disabled. Private composition now supports service JWTs behind revoked direct EXECUTE; public wrappers recheck staff after waits, and email/link contexts recheck their stored actor (links also recheck expiry). Evidence: `docs/evidence/canonical-api-release-confirmation-20260914.json`;73 SQL files/2,812 assertions,162 prescription harness checks,241 attachment harness checks (37 new checks across10 newly observed waits). New races cover staff revocation during operation/source/object waits, replay/read revocation, worker actor revocation and both source/correction confirmation orderings. `canonical-api-auth-storage-96-20260914.json` records120 actual local Auth/PostgREST/Storage capture/review/chart checks on96 fingerprinted migrations and verified cleanup. This HTTP suite does not yet cover schema9 physical release packaging. Remaining work includes source chooser and worker/rendering integration, mixed/max-count packages, document-link role/expiry wait acceptance, mapping mutation coverage, populated84-to-target upgrade/restore and coordinated hosted commissioning; no sends or release-policy activation occurred.

Release source discovery increment (2026-09-14): additive20260914100000 provides staff-only schema9 listing and select-all on canonical captures. Currentness joins exact ready request, approval, capture, intent, Storage identity and canonical source context, excluding superseded and stale records while chart history remains unchanged. Existing schema8 candidates, disclosures and selections are preserved. Listing uses the existing100-row pagination contract; select-all inspects at most21 eligible API IDs before rejecting overflow and checks the24-original package cap. Evidence: `docs/evidence/canonical-api-release-discovery-20260914.json`;544 assertions across9 targeted SQL files (18 new),177 existing attachment harness checks, owned cleanup. Maximum-count/multi-page fixtures, UI and shared renderer/worker integration remain pending. Disposable runner now requires97 migrations; the prior96-migration HTTP receipt is not proof of97-migration transport acceptance. No hosted changes or policy activation.

Release UI/shared-renderer increment (2026-09-14): the existing release screen now uses schema9 discovery/preview, current-format clinical acceptance, a separate candidate cache namespace and20API selection limits. The shared renderer validates canonical v2 approval/capture-v1 context, exact selected file descriptors and24-file/20API bounds; native source validation recognizes API digests only after that contract passes. Canonical prescription/vaccination/narrative validators remain active under schema9. Email and document-link builders accept only the canonical API bucket and retain byte hashing. Evidence: `docs/evidence/canonical-api-release-ui-rendering-20260914.json`;547 units (19 new),21 release browser tests (1 new),three overlapping post-cache-change browser checks,lint/typecheck/build and four affected Edge Function checks passed. Browser acceptance covers API selection and exact retry after a simulated lost confirmation response. A positive prescription/API fixture preserves partial disclosure; an in-memory email fixture assembles20API/24total originals and rejects overflow. Initial browser fixture lacked matching native candidate metadata and was corrected; the final suite passed. No hosted changes or sends. Actual Storage release preparation/recovery, SQL maximum-count/pagination fixtures, physical maximum packages, document-link wait acceptance, populated upgrade/restore and coordinated hosted commissioning remain required.

Actual physical-original email preparation/recovery (2026-09-14): the97-migration disposable Auth/PostgREST/Storage harness now creates an API-only release from the physically captured and corrected original. The actual SQL preview passes the production renderer, confirmation requires explicit local fixture acceptance, SQL capture rejects same-length changed PDF bytes, and the production preparation handler is exercised through local HTTP with real Auth validation. A deliberately lost response after successful payload capture recovers exactly one immutable payload without another Storage read. After a real source-head change, the release is ineligible but exact confirmation and prepared-payload recovery remain available without rereading or sending. `docs/evidence/canonical-api-release-physical-email-20260914.json` records134 checks (50 metadata,84 originals;14 new), fingerprints all97 migrations and shared delivery/rendering dependencies, and verifies container/volume/private-file cleanup. Lint passed with the existing warning. All provider requests remain synthetic; no outbox side effects occurred. This proves one API-original email preparation/recovery through local Node HTTP bindings, not deployed Edge routing, actual mail delivery, mixed native/API or maximum physical packages. Actual document-link preparation/recovery, role/expiry waits, mapping/max-pagination fixtures, populated upgrade/restore and hosted/provider/clinical acceptance remain outstanding.

Actual physical-original document-link preparation/recovery (2026-09-14): the same97-migration local Auth/PostgREST/Storage fixture now confirms an SMS-source API release, verifies document-link preview is denied without consent, then records explicit synthetic local consent. The production link-preparation handler runs through local HTTP with actual Auth validation. SQL independently rejects changed original bytes before capture; a deliberately lost post-capture response recovers exactly one immutable artifact and its physical-file digest without another Storage read. Actual source invalidation preserves historical prepared-artifact recovery while fresh review attestation fails. `docs/evidence/canonical-api-release-physical-link-20260914.json` records146 checks (50 metadata,96 originals;12 new), exact migration/shared-dependency fingerprints and verified cleanup. Lint passed with the existing warning. Initial attempts exposed a missing synthetic consent fixture and an assertion placed at medical-record preview instead of document-link preview; both test fixtures were corrected, and both failed environments/private logs were cleaned up before final acceptance. No outgoing message was created. This covers one API original through email and document-link preparation/recovery, not mixed/max physical packages, deployed Edge routing, public capability retrieval, actual delivery, role/expiry lock waits or populated upgrade/restore. Those and broader hosted/provider/clinical acceptance remain required.

Document-link retrieval wait authorization (2026-09-14): additive20260914110000 closes the later access-budget wait gap by checking the approving actor and wall-clock expiry immediately after updating the counter, before returning manifest or bytes. The existing schema9 source-wait guard from090000 is preserved. Concurrency fixtures now bind known synthetic PDF bytes through normal SQL capture/approval/release/link operations. `docs/evidence/canonical-document-link-wait-authorization-20260914.json` records73 SQL files/2,830 assertions,162 prescription and259 attachment harness checks (18 new checks across four observed role/expiry source/budget waits plus two positive retrieval checks). Denials roll back counter usage; current reviewed links still return exact bytes. `canonical-api-auth-storage-98-20260914.json` records146 actual local preparation/recovery checks and verified cleanup on98 fingerprinted migrations. This is SQL retrieval authorization plus separate physical preparation/recovery evidence; actual public HTTP capability retrieval is still pending, as are mixed/max physical packages, mapping/max-pagination fixtures, populated upgrade/restore and hosted/provider/clinical acceptance. No hosted change or sends.

Actual public document-link HTTP retrieval (2026-09-14): expanded98-migration disposable acceptance creates a separately reviewed link while preserving the unreviewed recovery fixture. The production public handler runs over local HTTP without staff Authorization, validates the materialized token against actual service context, returns an uncached manifest without private paths and delivers the exact physically captured PDF from the immutable artifact. Unreviewed packages, well-formed wrong tokens, another grant, an unselected artifact index, approving-staff role removal and subsequent source invalidation all deny access. Invalid tokens and stale-source denial preserve access budget; public retrieval adds no original Storage reads and no outbox messages. `docs/evidence/canonical-api-public-link-http-20260914.json` records159 checks (50 metadata,109 originals;13 new), exact98-migration/shared-dependency fingerprints and verified cleanup. Lint passed with the existing warning. This proves the production public handler through local Node HTTP bindings, not deployed Edge routing, browser capability UX, real SMS/email delivery or clinical/provider acceptance. Mixed/max physical packages, mapping/max-pagination fixtures, populated upgrade/restore and coordinated hosted commissioning remain required.

Mixed native/API physical release acceptance (2026-09-14): the98-migration disposable fixture now uploads and finalizes a distinct native PDF using staff Storage policy before the API capture side-effect baseline. The files deliberately have equal length and different content. Actual schema9 preview/confirmation preserves both exact descriptors. A cross-family byte swap is rejected by SQL capture without creating a payload; production HTTP email and document-link preparation retain separate exact file digests, and public HTTP retrieves each physical original correctly. Later API source invalidation blocks the mixed release/public package. `docs/evidence/canonical-api-mixed-physical-release-20260914.json` records171 checks (50 metadata,121 originals;12 new), exact98-migration/shared-dependency fingerprints and verified cleanup. Lint passed with the existing warning. Existing API-only, lost-response, consent and public authorization checks remain in the same run. This is a two-file native-document/API-original case through local Node HTTP bindings; positive prescription/API SQL composition, maximum physical packages, source-discovery overflow/pagination and mapping fixtures, populated upgrade/restore, deployed Edge routing and hosted/provider/clinical acceptance remain outstanding. No messages or hosted changes.

Current staging baseline upgrade/restore (2026-09-14): a fresh read-only staging ledger still contains84 migrations. Added a separately frozen `--rehearse-staging-baseline` mode; reconstructed84-version source is populated before the14 missing migrations, including three historical gaps, are applied to reach98. Actual database/physical Storage restore passes canonical routine/trigger/relation/RLS/default-privilege comparisons both before and after restoration, real Auth/private-file/immutability checks, and owned cleanup. A separate read-only comparison matches473 staging application routines and252 trigger bindings against the initial local baseline. `docs/evidence/canonical-staging-baseline-restore-20260914.json` records exact migration/runner/fixture hashes and comparison scope. Five inventory comparison tests pass. This does not yet populate schema9 approvals/cancellations or saved delivery artifacts for restoration; pre-upgrade data preservation covers the native clinical/billing/Auth/Storage fixture, with imported receipts/originals seeded after upgrading. Those expanded restore cases, hosted relation/RLS drift, max packages/discovery, and broader commercial acceptance remain required. No hosted mutation, push or sends.

Populated review-decision restore (2026-09-14): expanded the actual84→98 upgrade/restore fixture with an authenticated approval, correction and separately canceled decision bound to the physically uploaded canonical original. Both decision tables join the exact source/destination snapshot. After restoration, actual authenticated recovery and retries preserve both historical versions and canceled outcome; cancellation cannot overwrite an approved decision, and a delayed approval cannot revive cancellation. Service-authorized reviewed-original bytes still match, anonymous recovery/direct staff table reads deny, and privileged update/delete probes reject history changes. `docs/evidence/canonical-api-decisions-restore-20260914.json` records98 exact migration hashes, two approval rows/one cancellation, three recovered decisions, both canonical schema/access comparisons, physical restore and verified cleanup. Five inventory tests and JavaScript syntax check pass. Decisions are seeded after upgrade; schema9 confirmed releases/saved artifacts, source changes/disabled-policy recovery and broader hosted/provider acceptance remain required. No hosted mutation, push or messages.

Saved schema9 delivery-package restore (2026-09-14): added production-renderer email/link packages bound to the actual private API original. The owned fixture confirms separate EMAIL/SMS releases, captures immutable payloads, records synthetic SMS consent and reviews the link without queuing messages. Expanded snapshots cover release requests/payloads, link grants/payloads/events/counters, consent and conversation. After84→98 upgrade and actual restore, exact confirmation/preparation retries and stored byte hashes match; actual service RPC retrieval returns the saved manifest/original and increments only the known access counter by two. Independent rolled-back policy-disable and Animal-source-change probes deny release authorization/public retrieval while retaining staff recovery; source change emits invalidation and denied retrieval spends no budget. Exact snapshots compare before reads and after only the verified counter delta. `docs/evidence/canonical-api-release-packages-restore-20260914.json` records98 migration hashes, renderer/helper fingerprints, both canonical routine/trigger/relation/RLS/default-ACL comparisons, physical bytes and verified cleanup. Five inventory tests and both JavaScript syntax checks pass. The fixture now uses a real digest for its synthetic Animal payload; production validation was unchanged. Native records are populated pre-upgrade, imported/package rows post-upgrade. Max physical packages/discovery/mapping, deployed Edge and hosted relation/RLS parity, provider/clinical and broader commercial commissioning remain required. No hosted mutation, push or sends.

Hosted public-access audit (2026-09-14): added a read-only relation/policy/default-ACL inventory and strict comparator, captured the local84-version baseline before upgrade, and compared fresh staging metadata. The exact84-version ledger,191 relations,235 policies and six default-ACL records were inspected. All ordinary table grants (including MAINTAIN), ownership/RLS flags and policy definitions match. Five private history sequences have broader hosted USAGE/SELECT grants, while local and hosted both retain API-role UPDATE; three postgres/public default ACLs differ and even local defaults retain unnecessary future table privileges. `docs/evidence/canonical-staging-access-audit-20260914.json` records the failing hosted comparison, exact differences, source hashes and a passing full98-migration saved-package restore/cleanup. Ten comparator tests pass. `access-hardening.md` defines the additive correction and required creator-role, future-object, runtime and migration/restore acceptance. No hosted mutation occurred; the correction is not yet implemented. Hosted access parity remains an open gate, alongside remaining package/discovery and broader commercial acceptance.

Application-owner access correction (2026-09-14): additive20260914120000 removes API-role privileges from five private history sequences and explicit postgres/public table/sequence/function default grants. Existing table/RPC permissions, ownership, RLS, global defaults and managed creator defaults remain unchanged. Hosted read-only preflight confirms executor=postgres, no supabase_admin authority, and all191 relations/473 application functions owned by postgres; no broader managed-role access is required for this application scope. The99-migration stack passes74 SQL files/3,109 assertions, including93 hardening assertions on each final/local-drift/hosted-drift baseline;162 prescription and262 attachment concurrency checks;171 actual Auth/HTTP/Storage checks; and populated84→99 physical restore with both canonical access comparisons. Three post-migration startup/restore snapshots deny all API sequence privileges and postgres default API grants. Ten comparator tests pass. Initial concurrent test startup produced one unobserved source-lock wait; the unchanged full suite passed after other runs finished, and that limitation is recorded. Evidence: `public-access-hardening-20260914.json`, `public-access-hardening-physical-20260914.json`, `public-access-hardening-restore-20260914.json`. No hosted mutation or sends. Coordinated hosted application/parity, max physical packages/discovery/mapping and broader commercial acceptance remain required.

Positive prescription/API composition (2026-09-14): preserved the API-only restore package pair and added an EMAIL/SMS pair combining the corrected partial prescription with the API original through actual schema9 preview/confirmation and production shared renderers. Both source IDs/hashes and source-registry kinds are verified. Both restored reports retain the partial/missing-item disclosure and outside-history non-prescribing disclaimer, while physical original bytes and exact confirmation/preparation retries remain intact. Separate Animal and prescription source changes invalidate the combined release and deny authorization/public artifact RPC retrieval without changing saved package hashes or spending budget. Exact snapshots permit only two successful reads per link (four total). `docs/evidence/canonical-prescription-api-composition-restore-20260914.json` records99 migration/renderer/fixture hashes, two email/two link payloads, populated84→99 physical restore, both canonical access comparisons and verified cleanup. Ten comparison tests and both JavaScript syntax checks pass. The fixture uses one active household conversation for both package pairs. No hosted changes or sends. Max physical packages/discovery/pagination/mapping, deployed Edge and coordinated hosted/provider/clinical acceptance remain required.
