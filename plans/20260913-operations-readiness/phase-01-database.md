# Phase1 — safe discovery and scheduler receipts

Priority: launch readiness. Status: pending. Base: repository migration4300. Create one additive4400 migration, focused SQL assertions and concurrency coverage under `supabase/`. No hosted writes.

- Add active-ADMIN safe overview and keyset discovery for uncertain/failed outbox work, expired claims and pending queue age. Include IDs needed to open the conversation/invoice, channel/state, safe reason and timestamps; exclude recipients, bodies, private paths, capabilities, credentials and lease tokens. Use timestamps rather than invented overdue thresholds.
- Add paginated unfinished Stripe event discovery with deterministic timestamp/ID cursor and explicit state filters. Preserve existing read_stripe_event_queue contract for old consumers. Include exactly the safe fields required by existing reviewed retry controls; no provider payload expansion.
- Extract the existing queue_due_reminders candidate selection into a non-public read-only helper; preserve every source/template/version/date/channel/handoff condition and ordering. Both queue and backlog discovery consume it. Call candidates eligible for scheduler consideration, not guaranteed deliverable: final source/consent checks still apply. Include uncreated care jobs by source identity, not only existing job rows. Provide blocked-handoff pagination separately.
- Add minimal service-owned scheduler run identity and immutable start/completion/failure events. Record bounded requested limit, server timestamps and validated queued/blocked/skipped counts. A started run without completion remains unresolved; never infer zero work or failure. Use a caller-generated stable run UUID with idempotent start recovery. Provide a service-only wrapper that queues and records its result in the same database transaction; exact replay reads a completed receipt without queueing again. Do not rerun an unresolved invocation merely to reconstruct counts. Exact duplicate finish recovers original evidence; contradictory terminal results reject. Definitive failure requires evidence that the queue transaction did not commit; transport failure alone leaves outcome unknown. No raw exception text. A disabled handler must continue to make no database client or receipt.
- Overview links to existing inbound discovery and counts held/unassigned work without duplicating its mutation logic. Counts include an observed timestamp; pages may change with concurrent workers and do not promise a frozen snapshot across requests.

Absolute root for new files: `/Users/davidedler/livingroom-vet-operations-readiness-plan/`. Planned files: `supabase/migrations/20260913440000_operations_visibility.sql`, `supabase/tests/operations_visibility.test.sql`, and `supabase/tests/concurrency/operations-visibility.mjs` (adapt runner structure to existing tests). Do not edit prior migration files.

Acceptance: inactive/nonadmin/anonymous denial, service-only run writes, immutable exact terminal recovery, deterministic pagination beyond100 completed Stripe events with older held work, pending care sources without preexisting job rows, no raw-content projections, same scheduler candidate identities before/after extraction, existing reminder/outbox/payment regressions and contention passing. Explain indexes and bounded limits using actual query plans or fixtures. Review unresolved start/finish races before UI integration.
