# Phase2 — scheduler evidence and operations screen

Priority: launch readiness. Status: pending phase1 contract. Root paths below are relative to `/Users/davidedler/livingroom-vet-operations-readiness-plan/`.

Worker ownership: modify `supabase/functions/_shared/reminder-scheduler.ts` and its focused tests. Preserve method/auth/environment/body validation and the disabled-before-database boundary. Create a stable run UUID after valid enabled invocation and confirm its idempotent start receipt before queueing. Invoke the service-only wrapper that queues through the canonical function and records validated counts atomically. Recover that same receipt after an ambiguous response; a missing terminal receipt leaves outcome unknown, not failed or zero. Never queue until the start receipt is confirmed. Distinguish queue failure from lost completion acknowledgment; never automatically execute queue again to repair only a missing run receipt. Keep externally returned errors sanitized and `dispatched:false`. Add actual local HTTP/Auth/PostgREST evidence with synthetic reminder fixtures and no provider transport. Verify fixtures and run receipts clean up under test-only service authority.

UI ownership: create `src/hub/features/operations/` API/parser/page files, an ADMIN route and navigation link in the existing route structure. Reuse semantic tokens and shadcn; mobile-first. Show observed time, counts, oldest timestamps, safe state labels, bounded pages and links to the relevant conversation, reminder, inbound review and invoice workflow. Update the Stripe retry panel to consume the additive paginated discovery so opening older held work can actually reach its existing retry preview. Preserve that panel's original UUID/hash/reason recovery and draft guards.

Do not call a worker or enable policy from this page. Do not render a green health claim from an empty queue. Show unknown/no-run states explicitly, tolerate each independent section failure without presenting stale data as current, and clear actor-specific state on account changes. Avoid adding generic outbox retry buttons because ambiguous sends require existing evidence-based reconciliation.

Validation: parser/role/error tests; browser tests with more than one page, old unfinished Stripe event, empty/no-recorded-run state, safe link targets and no mutation on observation; runtime unauthorized/disabled/no-client behavior, successful and failed/uncertain run evidence; combined existing inbound/Stripe retry browser regression; actual local database workflow. Add only the needed runners to existing CI. Full lint/typecheck/unit/build before stacked PR integration. Hosted smoke tests and actual scheduled invocation remain explicit commissioning work.

Definition of done: authorized operators can discover and navigate old unfinished work, scheduler run evidence is durable and honest about uncertainty, existing queue semantics remain unchanged, and tests cover the real authority boundary. This does not complete external monitoring, automatic activation or commercial acceptance.
