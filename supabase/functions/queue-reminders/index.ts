import { serveDisabledLegacyFunction } from "../_shared/disabled-legacy-function.ts";

serveDisabledLegacyFunction({
  slug: "queue-reminders",
  replacement: "appointment reminder database enqueueing",
});
