# September 25 repair progress and release contract

Audited baseline: `7eccfaf0a1227358edb44e14cbb725e28fe97298`. The repair code is on `codex/commercial-readiness-repair-20260925` in commits `bdbe7ff`, `204b48f`, and `60d2e4c`. This is local implementation evidence, not a deployable or commercially accepted release. The [independent audit](2026-09-25-independent-commercial-audit.md) and [machine snapshot](2026-09-25-independent-commercial-audit.json) provide the hosted observations. Re-read hosted state before any rollout.

## Completed locally

- Added a forward migration after all 150 audited migrations. It removes the anonymous/authenticated/service-role table and column INSERT paths to `contact_submissions` and drops the permissive insert policy. The trigger still validates fields and protects immutable originals and triage attribution. Verified intake retains its exact replay receipt and five-per-hour claimed-email budget; the conflicting trigger duplicate/three-per-day checks are removed.
- Added grant assertions to the intake suite and revised the older triage suite to expect direct anonymous denial while proving trusted RPC insertion, guard validation, and staff triage still work. The three previously failing suites passed in rollback-only transactions against the existing local database with the new migration loaded. A fresh separate Supabase project then replayed all 151 migrations and passed the entire pgTAP suite: 118 files, 4,725 assertions. Its containers, volume, and temporary project directory were removed afterward.
- Home now shows loading, error, and retry states for each attention count. A failed schedule read cannot show “No visits scheduled” in the second panel. The commissioned navigation suite passed 7/7, including personal unread counts, retry recovery, desktop, and mobile.
- Release control now requires fresh evidence and successful frontend, Edge, and database CI jobs for the exact reviewed SHA. It blocks when deployed critical slugs lack matching source and behavior proof. `npm run check` passed locally; this does not provide a GitHub run or hosted behavior proof.
- Each summary gate now blocks on its own stale or missing evidence. Running the summary against the current artifact set reports all six gates blocked; it cannot call the old Hub snapshot a current pass.
- The read-only Edge reviewer now downloads five required existing slugs as well as remote-only slugs. A fresh inventory and source review on September 25 Pacific / September 26 UTC found all five critical entrypoints mismatched and calling `serveDisabledLegacyFunction`; all downloads succeeded. See [function inventory](2026-09-26-edge-functions-inventory.json) and [source review](2026-09-26-remote-edge-function-review.json). The temporary downloaded source was deleted after hash/comparison capture.

Local commands and results (the code tree in the three repair commits above): `npm run check` passed lint, typecheck, 1,085 unit tests, and build; `npx playwright test e2e/commissioned-navigation.spec.ts --reporter=line` passed 7/7; `supabase test db --workdir <isolated-project>` passed 118 SQL files and 4,725 assertions after full migration replay. The three initially failing SQL files also passed through `psql -v ON_ERROR_STOP=1` with the new migration prepended inside an outer transaction and final `ROLLBACK`. Browser tests use synthetic mocked network responses. No GitHub CI run exists for these local repair commits.

## Queue ownership for the candidate release

| Work | Canonical queue and entry | Required worker and proof |
| --- | --- | --- |
| Staff conversation email/SMS, including reviewed attachments | `prepare_message_request` → `enqueue-message` → `enqueue_communication` → `communication_outbox` | `dispatch-outbox`; frozen content/attachment bytes, consent, retry receipt, provider callback, and visible terminal state |
| Appointment reminders | `queue-reminders` → reminder enqueue RPC → `outbound_deliveries` | `dispatch-outbound-deliveries`; one reminder/delivery link, token authentication, lease/retry, signed callback, and visible terminal state |
| Public website inquiries | `public-contact` → challenge verification → `accept_contact_intake` | No messaging or household match on acceptance; staff triage begins from the immutable submission |

The separate queues are retained for this candidate because the staff outbox has saved-request and attachment contracts that the newer reminder worker does not implement. `send-email` and `send-sms` have no current browser callers, but their deployed bodies enqueue into `outbound_deliveries` while repository bodies return 410. Their cutover needs an explicit compatibility decision and route check; neither deployed behavior counts as staff-message acceptance. Keep provider send gates disabled until controlled synthetic acceptance.

## Staging-first deployment order

1. Capture fresh migration receipts, schema/access and routine inventories for both projects. The audited snapshot had 31 staging gaps and two primary gaps; this new forward migration increases the candidate gap to 32 and three respectively if hosted state is unchanged. The staging gaps are interleaved. Rehearse the exact ordered set and a populated restore using `scripts/restore-rehearsal/`; do not edit applied files or infer equality from receipt counts.
2. Apply and verify in staging first. Test direct INSERT denial as `anon`, `authenticated`, and `service_role`, plus verified RPC acceptance/replay, staff triage, and service-only SMS conversation creation. Compare effective grants, policies, definitions, and records to the rehearsal.
3. Build one reviewed Edge artifact manifest from the same commit. Include `enqueue-message`, `public-contact`, `dispatch-outbox`, `process-inbound`, `queue-reminders`, `dispatch-outbound-deliveries`, their shared modules, provider callbacks, and current attachment/estimate functions used by enabled UI. Explicitly mark optional `send-provider-email` and `suggest-replies` unavailable. Compare deployed source for existing slugs; ACTIVE and OPTIONS 200 are insufficient.
4. Reconcile cron routes and authentication for both queues before adding Vault secrets. Existing cron calls old workers and does not schedule `dispatch-outbound-deliveries`. Keep dispatch disabled while confirming target URL, secret/header, worker code, and outcome receipts. Exercise queued → claimed → provider accepted → signed callback → visible terminal state with controlled recipients, including uncertain-response recovery and SMS consent.
5. Configure the public contact build only after the verified endpoint, challenge hostname/action, server secrets, and test submission/receipt work in staging. The current production bundle has no configured intake URL. A configured URL alone is insufficient.
6. Obtain a successful GitHub CI run on the integrated SHA, then capture fresh readiness evidence. The expected CI JSON file is `docs/launch-evidence/YYYY-MM-DD-github-ci.json`, from `gh run view <run-id> --json headSha,status,conclusion,updatedAt,jobs`; the summary requires completed successful `frontend`, `edge`, and `database` jobs.
7. Confirm hosted backup retention, actual private Storage byte recovery, operator access and alert delivery. Carry out named clinical/staff decisions and provider round trips before considering production rollout. Production payment and live send gates remain disabled; CloudTalk/public phone are deferred.

Production deployment, scheduler configuration, provider sends, human acceptance, and hosted recovery remain unverified. No hosted SQL, function, credential, or provider setting was changed by this local repair.
