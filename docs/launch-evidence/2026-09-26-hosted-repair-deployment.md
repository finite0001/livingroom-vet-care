# Hosted repair deployment — September 26, 2026 UTC

Source package: [PR #205](https://github.com/finite0001/livingroom-vet-care/pull/205), branch `codex/commercial-readiness-repair-20260925`. The migration and Edge source applied here came from commit `b3d27b82c16860c545a8c42e5dff391057f3af21`. Its [CI run 36220713728](https://github.com/finite0001/livingroom-vet-care/actions/runs/36220713728) passed all three jobs: Edge, frontend, and database. A follow-up repository configuration change explicitly sets `verify_jwt = true` for `enqueue-message`; that setting was redeployed and verified on both projects. The final documentation/configuration commit must pass its own exact-SHA CI before frontend publication.

## Ordered hosted application

1. A private staging backup captured `public`, Auth, Storage metadata, and migration receipts, plus the original bytes of the one private Storage object. An isolated populated clone restored those records and bytes, corrected the two staging migration receipt collisions, and applied the exact 32 pending migrations. Its 205 existing public table digests remained unchanged except `appointments`, which gained a nullable `created_by` field.
2. Staging `kothoqicubowyhwfsrte`: applied the guarded [ledger correction](../../scripts/hosted-maintenance/2026-09-26-staging-ledger-correction.sql) in one transaction, preserving both SQL statement arrays. The next dry run selected the rehearsed 32 files, including the previously hidden native return and finance migrations. Applied all 32 with `supabase db push --include-all --skip-vault`.
3. Staging verification: 151/151 local migration versions and names matched. Live inventory reached 260 public relations, 870 routines, 428 triggers, and 239 policies. All 205 pre-existing public table digests matched the populated rehearsal; only the appointment digest changed from the pre-upgrade live baseline. Client, pet, appointment, Auth user, and Storage object counts remained 2, 1, 1, 2, and 1. The private object's 77 original bytes matched its backup SHA-256.
4. Staging Edge: deployed the explicit 23-function set below with one CLI invocation, not a blanket deploy. `public-contact` was already bundle-identical and the CLI left it in place. All 23 are active with the reviewed gateway JWT flags. The six launch-critical entrypoints downloaded back byte-identical to the repository. `health` returned 200; unauthenticated worker probes returned 401 except `dispatch-outbound-deliveries`, which returned 503 because its dispatch token is not configured. `public-contact` returned 503 because its challenge settings are not configured. None returned the prior 410 placeholder response.
5. A private primary-project pre-change schema/data backup was captured. Primary `mgadheotkdnrsatfivjy` had zero clients, pets, appointments, website submissions, and Storage objects, and one Auth user. Its 148 recorded migration names matched local files. Applied its three pending migrations: `20260924120000`, `20260924130000`, and `20260926031324`.
6. Primary verification: 151/151 migration versions and names matched, all listed record counts were unchanged, and direct contact submission INSERT privileges were absent at both table and column scope for `anon`, `authenticated`, and `service_role`. The trusted `accept_contact_intake` service RPC remained executable; an accept/replay/receipt probe passed inside an explicit rolled-back transaction in each project.
7. Primary Edge: deployed the same explicit 23-function set. All 23 are active, and the six critical entrypoints downloaded back byte-identical to the repository. Live unauthenticated probes showed `health` 200, `public-contact` 503, `dispatch-outbox`/`process-inbound`/`queue-reminders` 401, and `dispatch-outbound-deliveries` 503; none were 410. The initial primary deploy retained `verify_jwt = false` on `enqueue-message` despite the source matching. An explicit `supabase/config.toml` setting fixed this; redeployment and inventory confirmed `verify_jwt = true` on both primary and staging. The remaining selected JWT flags matched the reviewed configuration.

The explicit Edge set was: `enqueue-message`, `public-contact`, `dispatch-outbox`, `process-inbound`, `queue-reminders`, `dispatch-outbound-deliveries`, `resend-webhook`, `twilio-webhook`, `resend-delivery-webhook`, `twilio-message-status-callback`, `twilio-inbound-sms`, `verify-conversation-attachment`, `capture-conversation-email`, `capture-inbound-attachment`, `read-inbound-attachment`, `cleanup-abandoned-attachment`, `prepare-estimate-publication`, `recover-estimate-publication-preparation`, `read-estimate-publication-artifact`, `estimate-decision`, `prepare-estimate-decision-grant`, `recover-estimate-decision-grant`, and `health`. Repository `send-email` and `send-sms` are retired 410 stubs and were deliberately excluded from the deploy.

## Current release boundary

This is a backend repair deployment, not commercial acceptance. No provider message or payment was sent. The public inquiry form remains disabled, and the contact endpoint lacks its challenge secret/origin configuration. Worker/provider secrets, scheduled worker routing, controlled provider round trips, named clinical/staff signoff, and a tested hosted recovery arrangement remain outstanding. Both projects have completed daily physical backups in the reviewed CLI inventory but no PITR; the private local snapshots used here are not a durable recovery service. CloudTalk remains deferred, and Twilio/channel ownership still needs a commissioning decision.

The PR remains draft. The production frontend was not merged or published in this backend rollout; the Vercel preview requires SSO and has not had a remote browser smoke. The readiness report must continue to fail until exact deployed function behavior, hosted settings, provider receipts, recovery, and acceptance evidence are supplied. Do not switch payment or live-send gates on based on this receipt.
