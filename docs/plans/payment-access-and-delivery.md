# Reviewed invoice payment links and scoped client status

Status: next implementation contract, not delivered functionality. Integrate on `codex/payment-access`, which combines payment PR57 and document SMS PR56 without rewriting either published branch. Preserve existing v1 checkout attempts and their exact provider parameters.

## Intended client experience

Staff review an issued invoice's exact outstanding amount, recipient and full email/text before sending a Living Room Vet payment link. Default collection authorization lasts seven days (maximum seven days for this increment), independently of Stripe's shorter hosted-session lifetime. The client opens an isolated private page, sees only the reviewed amount and deadline, and explicitly chooses Continue to secure payment. No provider request is created by opening the page or by a mail scanner.

The client may return to the practice's scoped status page or reopen the message to see confirmed ledger status. Browser redirects, query parameters and cancellation navigation are never evidence of payment. Do not expose a client name, invoice UUID, services, diagnosis or other patient data through payment status.

A pre-created two-hour Checkout URL is insufficient for ordinary emailed invoices. Create a fixed provider session only after explicit client activation. A single reviewed collection authorization can reuse its unresolved attempt; after authoritative provider expiry it can create another attempt only while the original grant remains eligible and the exact invoice/source/amount is unchanged. Never rotate an idempotency key because a request timed out. If a previous attempt remains uncertain, reconcile it before any replacement.

## Immutable grant and attempt boundaries

Create a distinct payment collection grant, not a document grant. It freezes original staff actor, invoice/client, amount/currency, financial source hash, collection expiry, status expiry, approved origin, key version and separate collection/status token hashes. Use domain-separated HMAC capabilities; usable tokens and Stripe URLs never enter application storage, audit, query cache or browser storage. Status access expires thirty days after collection expiry and is narrower than the collection capability.

Preparation/capture/review follow the existing document-link pattern: exact request UUID recovery, service-only capability capture, explicit staff attestation before public activation and delivery. One unresolved captured/reviewable draft per actor/source; committed delivery receipts recover before current eligibility checks. Staff review can be recovered and revoked after source changes, actor identity remains enforced, and historical grants remain discoverable through safe metadata.

Do not lock an invoice merely because an unsent or unopened seven-day link exists. At every collection activation, lock grant and invoice, require an active originating actor, reviewed unrevoked unexpired grant, original invoice/source and exact outstanding amount, and reject unresolved collection/refund/reconciliation elsewhere. Changes to invoice credits or cash invalidate collection authorization instead of silently changing the amount. A paid invoice yields confirmed status and cannot create another session.

The activation RPC creates the provider attempt UUID and frozen creation/expiry/retry parameters atomically, using the same invoice lock and invariants as staff checkout preparation. Concurrent clicks reuse that attempt. Associate attempts immutably with the grant. A replacement is allowed only after accepted provider expiry (or a separately verified no-session reconciliation outcome), never from a local deadline alone.

New grant-generated attempts use a versioned v2 return context. Store only canonical success/cancel templates containing the grant ID and a status-capability placeholder, with immutable key/origin/context version. Materialize the exact return token in Edge memory before provider creation. Stripe will necessarily store the materialized return URL in its own session; application storage must not. Never retrofit v1 attempts or change parameters under an old idempotency key. Missing old keys blocks creation/materialization, while existing-session retrieval/expiry and provider reconciliation remain possible.

## Public and staff contracts

- Staff prepare/recover/capture/attest/revoke/history APIs expose safe metadata plus transient materialized payment message only after verified actor authorization. Supabase writes preserve the original authenticated staff actor.
- Public inspect validates the collection capability and returns reviewed amount/currency, deadline and coarse confirmed state without provider creation.
- Public activate validates the same capability, creates/reuses one durable attempt, reconciles known provider state, creates only eligible prepared attempts, persists evidence, then rechecks grant eligibility before releasing a transient Stripe URL. Revocation/source change during the network call must suppress the URL; it must not erase the unresolved provider attempt.
- Public status validates a separate status capability and returns confirmed cash/state scoped only to that grant and its attempts. It remains readable after collection revocation/expiry until its own deadline, subject to key availability. Pending webhooks display confirmation pending. Status reads never create a payment.
- Revocation prevents future activation/delivery and hides any new URL; it does not claim that a Stripe session already handed out was canceled. Staff must expire/reconcile any existing open session; UI exposes that distinction.

## Reviewed email and SMS

Messages contain an LRV collection capability URL, never a persisted raw Stripe URL. Freeze sender/channel/conversation/recipient, subject, exact message template containing one `{{payment_link}}`, amount/source and collection deadline. Explicit staff review binds the exact materialized-message hash. Outbox stores only template and hashes; the worker materializes in memory and the final attempt guard rechecks proof, actor, source, contact/consent, grant state and deadline. Preserve existing invoice-email/document-SMS guards and ambiguous provider recovery.

Offer reviewed invoice attachment for email using the existing frozen invoice renderer/capture checks; attachment/source review must remain bound to the same invoice and amount snapshot. Do not put a payment token into an immutable stored HTML/PDF. Extend canonical incoming-token redaction only after signature verification and reject capabilities at persistence boundaries. Do not claim arbitrary encoded attachments are scrubbed.

## Validation and rollout

Use default-off public collection/status flags and existing independent provider/collection/refund/receipt/worker switches. Isolate `/pay/` and scoped payment return/cancel routes before importing staff Auth or marketing resources; strip fragments immediately, use no-store/no-referrer, explicit action, bounded responses and memory cleanup.

Required evidence: actual database concurrency for repeated activation and credit/payment races; exact return-key/version recovery; lost provider response without replacement; expired session renewal only after authoritative evidence; revoked/source-changed grant during provider work; explicit review and same-request delivery recovery; quoted capability redaction; public cross-grant denial and forged paid flags; browser draft/URL privacy; actual local HTTP/storage/worker wiring; real Stripe sandbox payment/refund/webhook acceptance and configured provider email/SMS acceptance. Local mocks cannot establish provider readiness.

References checked September 12, 2026: [Checkout creation](https://docs.stripe.com/api/checkout/sessions/create) permits a fixed expiry between thirty minutes and twenty-four hours; [idempotency](https://docs.stripe.com/api/idempotent_requests) requires identical parameters and a bounded retry horizon. The longer-lived LRV authorization is an application workflow, not a claim that a Stripe session lasts seven days.

## Delivery database foundation (3700; local verification)

Migration `20260913370000_payment_link_delivery.sql` adds immutable `payment_delivery_requests`, service-only `payment_delivery_captures` and reviewed `payment_delivery_outbox_links`. This is a database foundation; the staff composer, service materializer and outbox worker adapter must be integrated before sending. No provider is configured or contacted by this migration.

The original grant owner prepares a distinct delivery UUID through `prepare_payment_delivery(p_request_id, p_grant_id, p_conversation_id, p_channel, p_recipient, p_subject, p_body_template, p_invoice_email_request_id default null, p_invoice_payload_hash default null)`. It freezes the grant's invoice/client, financial source and exact amount; the template contains exactly one `{{payment_link}}`. SMS has an empty subject and a 1,600-character template limit; email has a required subject and a 100,000-character template limit. Current household recipient, conversation, suppression/consent, active actor, reviewed grant, deadline and collectible financial state are checked. Exact UUID retries recover before current eligibility; changed intent cannot reuse the UUID.

`recover_payment_delivery(p_request_id)` returns safe request metadata, immutable capture metadata and the actual outbox receipt, scoped to the originating active staff actor. It does not rematerialize a capability or require the grant to remain deliverable merely to recover history.

Service flow:

- `payment_delivery_capture_context(p_request_id, p_actor_id)` returns `{request, grant, capability, invoice_payload, invoice_payload_text, capture}` after current eligibility validation. `request.amount_cents` is a decimal string. `capability` is the stored collection capture, including capability context and token hashes; this response is service-only. `invoice_payload_text` preserves the original serialized invoice attachment payload for exact hash verification; its JSON view is also provided. Both are null without an attachment.
- The adapter verifies configured sender and materializes the collection URL only in memory. It computes `message_hash` from the exact materialized message and `payload_hash` from the exact transport payload, including sender, recipient, subject and optional attachment bytes. It must verify the original invoice payload hash before using those bytes.
- `capture_payment_delivery(p_request_id, p_actor_id, p_sender_config, p_message_hash, p_payload_hash)` stores only frozen sender metadata and the two hashes. Sender keys are exactly `{from, reply_to}` for email or `{from, account_sid}` for SMS. Configuration is bounded and contains strings only. The service must supply its configured sender; these fields are not a staff-controlled sender override.
- After reviewing the exact transient message and optional invoice attachment, the original staff actor calls `enqueue_payment_delivery(p_request_id, p_reviewed_message_hash, p_reviewed_payload_hash, p_attest)`. True attestation and both exact hashes are required. The immutable outbox link is the review/queue receipt. Queue retries return an existing receipt before current eligibility checks, including after grant revocation.
- The leased worker calls `payment_delivery_context(p_outbox_id, p_lease_token)` for the same service-only context and capture, materializes the same exact bytes and calls `start_communication_attempt` with sender metadata plus `payment_delivery_message_hash` and `payment_delivery_payload_hash`. The new outer guard rechecks source, review, contact/consent, grant and deadline, rejects missing or changed proof, and strips both proof keys before invoking the existing document/invoice/reminder/outbox guards. Existing workers therefore fail closed for these deliveries. The ordinary lease, uncertain-send and provider idempotency rules remain in effect.

Optional reviewed invoice attachment is included in this increment. Prepare/capture the existing 2700 invoice email artifact first, using the same subject and payment template without a token. Reference its request UUID and reviewed original payload hash when preparing payment delivery. The attachment must remain ready, unpurged and bound to the same actor, invoice, household, conversation, recipient and message; its current invoice renderer context and sender must match. The worker uses the frozen HTML attachment, not a new render. Supported decoded HTML is checked for private capabilities; no claim is made about scrubbing arbitrary encoded attachments. Attachment reuse does not enqueue a separate invoice email.

Local verification: 29 dedicated SQL assertions pass, covering exact preparation/recovery, template/token rejection, missing SMS consent, authorized SMS, actor/lease restrictions, reviewed email attachment and sender matching, queue replay, old-worker fail-closed behavior, proof-key stripping, source changes before dispatch, revocation and append-only/RLS protection. Existing document SMS 62, invoice email 55, communications outbox 44 and payment access 53 assertions pass. Tests use synthetic fixtures and roll back. Dedicated concurrent delivery/revocation tests, real materializer/worker roundtrips, staff UI and provider acceptance remain necessary integration evidence; this increment does not claim delivery readiness.


### Payment delivery transport integration

`payment-delivery-payload.ts` reconstructs the collection capability transiently and verifies both stored collection/status token digests against the frozen grant. It binds grant owner, invoice, household, financial source and exact cents to the delivery request. Email and SMS payload digests cover the exact transport body. Optional invoice attachments use the original reviewed2700 payload bytes/hash; their rendered HTML is preserved and rejected if it contains a usable capability.

`dispatch-outbox` loads3700 context only when `PAYMENT_DELIVERY_ENABLED=true` (default off). It also requires configured payment access keys and both public collection/status flags. It checks the outbox template, recipient, subject, channel, frozen sender, message digest and payload digest before the final database attempt guard. Only proof hashes enter attempt metadata; usable links remain in memory and the provider request. Existing workers cannot send3700 rows without proof. Existing document and invoice delivery paths retain their behavior when payment delivery is disabled.

Validation: 289 unit tests pass, including six payment materialization/worker tests and the existing document-SMS, invoice and release-email regressions. Deno checks the real dispatch entrypoint; focused lint passes. These use synthetic provider transport. Staff preparation/review UI, actual database-to-worker roundtrip, revocation concurrency tests and authorized real-provider acceptance remain outstanding. No sending flags, credentials or hosted schema were changed.
Additional integration review: `payment_delivery_concurrency.py` passes 21 checks across four observed PostgreSQL contention cases: revoke before dispatch start, dispatch start before revoke, invoice credit before dispatch start, and simultaneous enqueue retries. Revocation/credit winners prevent an attempt; a previously authorized start retains its attempt history when revocation follows. Exact queue retries create one outbox record, and proof digests never enter persisted sender metadata. All random fixture rows are cleaned afterward; no provider requests occur.

Actual database context review against runtime `2bab4e7`: a rolled-back service fixture confirms `grant.id`, string amount and `capability.grant_id/context_version` match `paymentGrantFromContext`, including the canonical captured context. The runtime rejects the fixture's deliberately synthetic nonmatching token hashes. Substituting independently derived synthetic hashes only in memory then verifies message materialization; this is contract evidence, not a captured provider acceptance test. No usable token was printed or stored. The runtime verifies exact original invoice payload bytes/hash and now checks the expected invoice HTML filename before transport reconstruction. No additional finance or fail-closed metadata mismatch was found in this bounded review.

Delivery history discovery (3900): authenticated `list_payment_deliveries(p_invoice_id, p_grant_id default null)` returns `{invoice_id, client_id, grant_id, deliveries, has_more}`. Each delivery is the existing safe recovery envelope; usable capabilities, attachment bytes and worker context are excluded. Results are limited to the original active actor and current invoice household, ordered newest first with a 100-row limit. A supplied grant must belong to that actor and invoice/household. Revoked, stale and queued history remains discoverable; no current send permission is inferred. UI must validate returned invoice/client/actor before displaying it. The dedicated history SQL fixture passes 41 assertions (26 shared delivery setup assertions and 15 discovery assertions), including actor isolation, grant/invoice mismatch, pagination, revocation history and anonymous/service denial.
