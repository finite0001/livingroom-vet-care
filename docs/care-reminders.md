# Reviewed care due plans and unsent reminder jobs

This module supplies `PatientVaccineDuePlans({ petId, onDirtyChange? })` and `CareRemindersPage`. It does not dispatch messages or enable a production scheduler. All job statuses are **pending (prepared, unsent)** or **invalidated**; there is no sent/delivered state or provider call in this implementation.

## Clinical due plans

Administrators define a canonical vaccine group key, exact catalog vaccine-product mappings, a standard interval and clinical-review rationale. No products, antigen equivalences, vaccine schedules or medical intervals are seeded or inferred from names/brands. The group key is immutable and normalized; staff must review the taxonomy so aliases do not represent the same antigen twice. A product may be explicitly mapped to multiple groups when the practice deliberately documents a combination product's coverage. The database allows only one nonretired plan per patient/group, including proposed plans.

A patient plan records its reviewed template snapshot/version, exact product, administration anchor, source, standard proposal, patient interval, reviewed next-due date, plan state and reminder eligibility. A linked administration must belong to that patient/product and must not be corrected; its anchor date is derived in America/Denver and verified against the submitted date. Historical anchors require explicit source provenance and a reviewed product mapping; unsupported legacy brand text is never automatically equated with a product. The UI calls these administration anchors, not an assertion that every external historical administration is known.

The standard-template proposed date remains distinct from the reviewed patient date. Deviating from the standard interval or calculated patient date requires a reason. Proposed plans are ineligible for jobs. Current plans require staff review and an explicit reminders-enabled setting. Retirement preserves history and prevents resurrection; a new reviewed plan may replace a retired one. Existing plans retain their prior template snapshots when a standard is edited or retired. The UI offers explicit adoption of a newer active version. No retroactive recalculation occurs.

Corrections to linked treatment records automatically demote affected plans to proposed, disable reminder eligibility and append a version explaining that the anchor needs review. No replacement date is guessed. Actual patient treatment history remains in its original ledger.

Every plan/template change stamps its authenticated actor and appends an immutable snapshot. RPCs enforce active staff, with ADMIN additionally required for practice settings. Stable UUIDs and expected versions protect unchanged retries and conflicting edits. Direct mutation/deletion of plans, templates and job history is not granted to browsers or service users. New/changed due plans require active patients. Technical interval bounds are not clinical recommendations.

## Service enqueue contract for the next worker

The migration exposes these functions only to `service_role`; the browser cannot enqueue or discover candidates:

- `list_care_reminder_candidates(p_through date, p_after_kind text default '', p_after_id uuid default zero UUID, p_limit integer default 100)` returns `{source_kind, source_id, source_version, pet_id, due_on}` in `(source_kind,source_id)` order. Limits are 1–500. Supply the last returned kind/ID for the next page. `p_through` is a finite **due-date horizon**, not a delivery timestamp. A worker using templates with `days_before` must set that horizon accordingly, then check the calculated scheduled date. Current enabled vaccine plans and planned/ordered native lab orders with due dates are eligible; inactive patients are excluded. Lab intervals remain solely in the existing lab module.
- Read `care_message_templates` as the service user. Administrators approve plain-text templates with channel, explicit days-before offset, version and review rationale. Only `{{patient_name}}`, `{{care_name}}` and `{{due_date}}` are supported. Rendering substitutes each original placeholder once; braces in patient/care names remain literal and are not interpreted recursively. No HTML template execution is performed.
- Call `enqueue_care_reminder(p_id uuid, p_source_kind text, p_source_id uuid, p_expected_source_version integer, p_message_template_id uuid, p_expected_template_version integer)`. Retain `p_id` across an unchanged retry. The service must explicitly choose the reviewed template(s) and schedule policy; this module does not select a template by guessing. The server locks and revalidates patient/source/template, derives the household, due date, scheduled date and rendered text, and returns one durable row. A second request ID for the same source/template versions returns the existing canonical job. Changed reuse of an ID fails; stale versions fail. Multiple deliberately approved template schedules can produce different jobs; the future worker must not select redundant templates for the same intended notification.
- Read `care_reminder_jobs` as the service user to reconcile an uncertain enqueue response. The key is `(source_kind, source_id, source_version, message_template_id, message_template_version)`. Source and message snapshots are retained; a pending job is never rewritten. An invalidated job is never resurrected. Changing patient identity/household may invalidate a job without changing its source version; that job requires a newly reviewed source/template revision, not reuse of its old message.

No cron, HTTP edge endpoint, job-claim lease, outbox insertion, recipient resolution or delivery acknowledgment is supplied yet. Before any future send, a worker must atomically revalidate current source/patient/template/job state, consent, suppressions, current destination, approved deployment/provider settings and its selected schedule; then hand off through the durable communication outbox with idempotency and honest delivery states. Queue presence alone is not authorization to contact a client. Do not treat the current `pending` table as a send-and-mark-sent API.

Pending jobs are invalidated on any vaccine-plan or lab-order revision (including rescheduling, cancellation, disabling and retirement), message-template revision/retirement, and patient archival/death/name/household changes. Linked vaccine administration corrections revise the plan and therefore invalidate jobs. Existing appointment reminders are shown as a separate read-only queue; their existing appointment-version invalidation continues, and no duplicate appointment jobs are generated here.

## UI integration and validation

Root should mount the patient component with `key={petId}` and connect its dirty callback to PatientPage's single guard. Wire the standalone `CareRemindersPage` at `/hub/tools/care-reminders` as a staff route (the browser test uses this URL); its own guard protects administrator settings. The dashboard shows upcoming 30-day, overdue or all current vaccine/lab dates, excludes inactive patients, and paginates up to 20 entries from each source. Latest care jobs and pending appointment jobs are separate bounded views with truthful labels. This commit does not modify PatientPage, App or shared generated types; browser tests used temporary mounts removed before commit.

Dr. Susan Edler must approve group taxonomy, exact product mapping, intervals, override workflow and clinical wording before practice use. Review notes are administrator/staff attestations, not an inferred clinical signature. Tests use synthetic data and no external messaging. SQL tests cover product/patient boundaries, canonical-plan uniqueness, version conflict/replay, override reasons, reviewed-snapshot preservation, retired/disabled sources, corrected treatment anchors, inactive patients, service discovery and job invalidation. Browser tests cover explicit mapping and due overrides, stale-draft recovery, and mobile administrator wording review without dispatch.
