# Prescription review and releases — staging rollout

September 13, 2026. Target `kothoqicubowyhwfsrte`. PR121 application revision `62ae162` passed all three CI jobs in [run34789393868](https://github.com/finite0001/livingroom-vet-care/actions/runs/34789393868), including459 unit tests and245 browser cases. The first run exposed two external-record browser fixtures still using the previous release RPC; updating those fixtures restored the acknowledgment/recovery checks. The six focused cases also passed locally.

## Installed and verified

Two explicit-project dry runs identified exactly migrations5700–6300. The CLI applied those seven migrations with `--include-all --skip-vault`, preserving the existing9000 receipt. Staging now has84 migration versions. The three new review/history tables have RLS and no anonymous select or authenticated/service direct insert privileges. All26 newly defined functions have pinned search paths; fifteen internal functions deny every API role, and eleven staff RPCs grant authenticated execution only. Actual calls under the authenticated database role without a staff identity rejected all eleven with42501 in a rolled-back transaction.

Seven export/delivery functions are ACTIVE version5: dispatch-outbox, prepare-document-link, prepare-invoice-email, prepare-payment-delivery, prepare-release-email, recover-document-link and retrieve-document-link. Their36 distinct downloaded source/configuration files exactly match deployed revision `826804c`, whose application and migration sources match the tested PR121 revision. JWT settings remain unchanged. All five staff preparation/recovery endpoints return401 to anonymous requests. No importer deployment was needed.

The [protected frontend](https://livingroom-vet-care-o6cr33wjr-daves-projects-e0da43ba.vercel.app) is READY, deployment `dpl_4baTe2QcrRQxmoCvvbn5NWd3yVjB`, target `staging`. Its guarded build confirms the staging backend and disabled contact intake. A separate local build from the exact446-file upload manifest passes; six deployment-configuration tests pass. Upload exclusions retain shared clinical renderers while excluding local environment files, migration/configuration files and test fixtures. Served entry and patient bundles verify the staging backend, schema8 RPC and prescription selection. Authenticated Vercel access to the login route returns200; unauthenticated access returns401. Project protection remains `all_except_custom_domains`, and automatic Git deployment remains disconnected.

The [sanitized deployment evidence](evidence/prescription-review-release-staging-20260913.json) records migration hashes, exact function manifests, grants and HTTP checks. Auth users, clients, pets, import runs, Storage objects, release policies and outgoing messages remain zero. No provider request, secret change, source-scope expansion, clinical-policy acceptance, outgoing delivery, main merge or public-domain cutover occurred.

## Security advisor triage

The advisor reports63 [private RLS tables without policies](https://supabase.com/docs/guides/database/database-linter?lint=0008_rls_enabled_no_policy), up three intentionally private ledgers; seven anonymous security-definer findings remain unchanged. The226 [authenticated security-definer findings](https://supabase.com/docs/guides/database/database-linter?lint=0029_authenticated_security_definer_function_executable) include the eleven new staff RPCs verified above. Their execution grants are intentional and their internal staff checks remain required. No broad access policies were added to silence these notices; this is targeted review, not resolution of all baseline advisories.

## Restore and remaining work

Concurrent stack conflict: [PR122](https://github.com/finite0001/livingroom-vet-care/pull/122), opened during this rollout at head `fa030cbb616edf25fb96d014807609c173a35bd7`, independently implements prescription releases. Its `20260913630000_prescription_release_validation.sql` reuses the version already occupied here by `20260913630000_record_release_prescription_history.sql`, followed by a separate6400 release migration. Do not apply that branch as-is or rewrite the deployed6300 receipt. Reconcile its implementation with PR121 and express any required differences as new additive migrations, then validate against this actual84-migration staging baseline. PR123 is stacked on PR121 and records that deployed baseline; PR122 was not merged or deployed by this rollout.

The fresh populated51→84 [restore rehearsal](evidence/fresh-gap-restore-local-20260913.json) passed with exact post-restore comparison of473 functions,252 triggers,191 relations,235 policies and six default-privilege records. Prescription reviews/corrections, frozen releases, prior clinical/billing/Auth records and private file bytes were preserved. Owned resources were removed. The runner now performs this post-restore check for both fresh and resumed gap rehearsals.

Hosted access and bundle checks do not establish a signed-in staff workflow or Dr. Edler's clinical acceptance. Live source samples, clinical interpretation approval, controlled delivery and broader commissioning remain pending. Next implementation is [patient-scoped attachment metadata intake](../plans/20260913-ezyvet-clinical-import/phase-03d-attachment-intake.md); original-byte capture and full migration reconciliation remain separate unfinished work.
