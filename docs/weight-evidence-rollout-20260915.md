# Weight evidence rollout — September 15, 2026

PR142 passed frontend, Edge and database CI on `a6c8638` (run35057545695) and merged as `e40dbee`. Both staging `kothoqicubowyhwfsrte` and primary `mgadheotkdnrsatfivjy` now have113 migrations through `20260916020000`. CLI applied only that migration, without seeds or roles.

Fresh archives of both hosted databases restored locally. Each rehearsal preserved235 existing table fingerprints and passed58 weight SQL assertions, with temporary databases removed. Local restore normalized ownership and omitted provider-owned default ACL entries; explicit grants were retained. This is not hosted owner/default-privilege parity or physical hosted Storage recovery evidence.

Both hosted function bodies match the tested hash. Authenticated execution is granted, anonymous and service-role execution denied; security-definer search_path is public. Client, patient and weight fingerprints are unchanged; migration runs remain empty. No provider gates were enabled or messages sent.

Local acceptance passed603 unit tests,37 workspace browser tests,141 authenticated HTTP checks including eight weight cases, and a populated additive upgrade/database/private-Storage restore. The read-only panel distinguishes approved created/linked measurement, unknown observed source versions, local measurement matches, patient/household drift and paginated source-change acknowledgments. An acknowledgment never promotes or replaces a weight.

Lovable confirmed “Your website was updated.” The public index `/assets/index-B-Ceqvar.js` selects only the primary backend; its import chunk `/assets/EzyVetImportPage-UpbVK9Lp.js` contains the weight RPC and panel. [Sanitized rollout evidence](evidence/weight-hosted-rollout-20260915.json) records hashes and backup/restore receipts. Supervised real-source acceptance, global outcome totals, operational resolutions, frozen reports and other commercial-readiness gates remain unfinished.
